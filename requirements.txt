pyboy>=2.0
Pillow

# Optional: prints the connection URL as a scannable QR code in the terminal.
# Everything works without it; you just have to type the URL.
qrcode
ruff

# Optional: `tools/coverage`, which ranks the modules by how much of each has
# never been run. The suite itself needs none of it.
coverage
